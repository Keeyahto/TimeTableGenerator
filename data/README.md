# Data directory

## Safe to commit

- `samples/synthetic-small/` — fictional minimal JSON for dev/tests only

## Local only (ПД / institutional data — never commit)

- `solver_agent_full_handoff_v2/` — real XLSX-derived audits, names, workload
- `phase 1/`, `phase 2/` — local working copies

Copy handoff from secure storage into `solver_agent_full_handoff_v2/` on your machine. See `docs/agent-handoff.md`.
