{
  "name": "schedule-solver-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "packages/shared-contracts"
  ],
  "scripts": {
    "dev:web": "npm run dev -w web",
    "build:web": "npm run build -w web",
    "lint:web": "npm run lint -w web",
    "validate:contracts": "npm run validate -w @schedule/shared-contracts",
    "prisma:migrate": "npm run prisma:migrate -w web",
    "prisma:studio": "npm run prisma:studio -w web"
  }
}
